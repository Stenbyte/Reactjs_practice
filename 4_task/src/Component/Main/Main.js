import React from 'react';
import Section from '../Section/Section';
import Form from '../Form/Form.js';

const main = () => {
    return (
        <main>
            <Form />
        </main>
    );
};

export default main;